# HANDOFF — 2026-08-09, the founding day's build arc (S2–S9, one continuous session)
*For the next session (any milestone) and for the operator. The tail of
`SESSION_LOG.md` remains the state of truth; this file is the day's map and the
judgment calls that don't fit a log entry. Repo: `github.com/bochen2029-pixel/fusion`,
branch `main` (work branches `m0-burn`, `m05-thesis-slice`, `m1-shape` merged at gates).*

## The arc (what happened today, in order)

| leg | commit(s) | outcome |
|---|---|---|
| Fresh-eyes audit | `abac78f` | 10 findings missed by all five QC lanes (2 gate-class); adjudicated with the founding session; D-018–D-020 |
| Spec v0.2 | `f55fb25` | canon bumped; FUSOR-1 pinned; contracts v1; all 146+ findings dispositioned; M0 unsuspended |
| Rulings + publication | `87e61b5` | tier-2 cut, MIT, repo public |
| **M0 — the burn** | `f23f7b7`/`e11d6b7` | **F-BURN-0 GREEN attempt 1: 985/1000, Wilson LB 0.975**; goldens; 3 CEM lessons (D-032h, D-033) |
| **M0.5 — thesis slice** | `1c0b35a`/`a77b566` | the triple watches the plant; **three-arm first data** (D-034); F-VERACITY grader; tokenizer-null confabulation datum |
| **M1 slice 1 — the enemy** | `d7218aa`/`0bbf34c` | γ⁻¹ 48.9 ms REPORTED from the 27-state eigenproblem; VDE death chain on tape; innovation lane LIVE (D-035) |
| **M1 slice 2a/2b-1 — the shape** | `3239653`/`857f890` + this commit | Δ* solver verified 4e-6 (Solov'ev); shaped equilibrium: **shift +27 mm, q0 1.08, q95 3.61** (D-036/D-037) |

Snapshots: `C:/fusion_snapshots/20260809-{133301,152707,154745,160954,162709,+this}`.

## What runs right now

`fusor` (verbose single run) · `fusor_mc` (the MC gate: Wilson CIs, verdict taxonomy,
goldens, `--verbose-fails`) · `fusor_shadow` (feed gen/compose/tape, three LLM arms,
F-VERACITY code grader) · `trainer/tune_pid.py` (CEM + playoff + seed guard) · ctest
**7/7**: `replay_oracle · physics_sanity · budget_events · golden_check · seedcheck ·
vertical_enemy · gs_solver`. Build block in `KICKOFF_M0.md`/`KICKOFF_M1.md` (VS 17 2022,
`/fp:strict`, tomlplusplus vendored, llama import libs delay-loaded from `C:/llama.cpp`).

## M1 — what remains, in recommended order

1. **THE MERGE (start here):** `vertical.h`'s calibrated `k_dest` becomes DERIVED from
   `gs_shaped`'s equilibrium field (decay index n = −(R/B_z)·∂B_z/∂R at the axis from
   ψ derivatives). Closes report-don't-configure end-to-end and makes γ move with the
   equilibrium through ramps — which M1's lethal-legal scenarios need. (~1 session.)
2. **Free-boundary + von Hagenow + X-point** on the verified core (coil Green's tables
   from `ring_mutual` — already in `vertical.h`); DST/CR fast solver behind the same
   `gs_solve` interface; then the D-003 pipeline (solve@T applies@T+50) into the tick
   loop with `gs_late` events. (~2–3 sessions.)
3. **Model-based EKF + χ² NIS** on the GS linearization (the kinematic α-β tracker is
   the stand-in; its measured failure modes are in D-035 — don't repeat: the reduced
   model must not carry raw stiffness on the slow manifold). Innovation gate recalibrates
   on it (v0 tokenizes above-dance kicks 11/20 — the honest current state).
4. **1-D transport + diagnostics noise into the burn loop; statecheck fence as ctest.**
5. **The oracle (CEM teacher, decorrelated stream) → F-SHAPE-1 attempt.**

## Open judgment calls (decided by whoever gets there, with these notes)

- **The PD null's ±2–4 cm limit cycle** (measured, D-035): an LQ vertical null (spec §3
  names it) will likely kill it — and that becomes the real M2 baseline. The limit
  cycle is F-NULL-C headroom evidence; keep the receipts.
- **Kd through the 10 ms actuator lag destabilizes** — any vertical controller redesign
  must respect that (or add lead compensation; sweep mode in `test_vertical` maps it).
- **q0 ≈ 1.08 at pbar=0.10 proxy**: real q0 needs axis-resolution work; sawtooth model
  (q0<1 crossing) should key off a proper axis value — flag for transport slice.
- **easy.toml runs hot** (T≈10–11 keV under the CEM's hot-hold gains): fine for M0's
  gate as receipted, but M1 scenario design should decide whether T_set enforcement
  tightens (Ki was ~0 by CEM choice — the offset is the strategy, not a bug).
- **M0.5 composed-session script timing**: anchor operator post-mortems to actual
  death events when regenerating feeds (D-034g).

## Estate-portable (for the founding session's next auricle touch — not this repo's edits)

1. Three-arm claim-separation → the whitepaper's F-PRESENCE (already queued at S2).
2. Citation-fidelity audit → auricle soak briefs (already queued).
3. **NEW: the M0.5 pair of data**: event-twin parity at 3.1× cost on tokenized lanes
   (D-018's narrowed claim, first measurement) + the tokenizer-null
   citation-vs-confabulation datum — both belong beside F-PRESENCE in the whitepaper.
4. **NEW: the misaligned-objective + playoff-selection lessons** (D-032h) generalize to
   any estate optimizer-vs-falsifier setup.

## Gotchas that cost time today (do not rediscover)

`NOMINMAX` before windows.h · TOML basic strings and inline tables are single-line
(tomllib-validate every contract before building) · python writes cp1252 unless told
`encoding="utf-8"` · `grep -c` with zero matches exits 1 and kills `&&` chains ·
Euler-map power iteration inflates oscillatory modes (use RK4 log-slope) · a reduced
estimator must not carry raw unstable stiffness on the slow manifold · Windows Git Bash
eats backslashes — forward slashes in every command, always.

## For the operator

The demo-visible assets already on disk: `runs/m05/*.tsv` + ledgers (the triple
watching, the Skeptic catching the planted falsehood, the Sentinel's gated would-fire)
and `runs/m1s1/feed_kick.tsv` (the innovation lane). Usage arithmetic for planning:
this day consumed one ~800k-token session for seven legs; M1's remainder is ~4–6 more
sessions at similar density; M2–M5 per the v0.2 estimates (ROADMAP). The bet's early
evidence (D-034) is already citable.
